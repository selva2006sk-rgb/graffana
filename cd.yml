apiVersion: argoproj.io/v1alpha1

kind: Application

metadata:

  name: three-tier

  namespace: argocd

spec:

  project: default

  source:

    repoURL: https://github.com/selva2006sk-rgb/graffana.git
    targetRevision: HEAD

    path: kubernetes

  destination:

    server: https://kubernetes.default.svc

    namespace: production

  syncPolicy:

    automated:

      prune: true

      selfHeal: true
